@page
@model WebApp.Pages.GpsLocations.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GpsSession[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GpsSession[0].CreatedAt)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GpsSession[0].SessionLength)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GpsSession[0].Duration)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.GpsSession) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CreatedAt)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SessionLength)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Duration)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.GpsSessionId">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.GpsSessionId">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.GpsSessionId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
